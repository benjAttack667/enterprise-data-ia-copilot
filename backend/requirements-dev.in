# Backend test dependencies. Runtime requirements stay defined in requirements.in.
# Regenerate both universal locks with:
#   python backend/scripts/lock_dependencies.py

-r requirements.in

pytest>=9.0.3,<10.0
httpx>=0.28,<1.0
